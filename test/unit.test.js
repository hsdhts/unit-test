import {sum} from '../src/unit'

test("sum function 1", () => {
    const result = sum(1, 2)

    expect (result).toBe(3);
})
test("sum function 2", () => {
    const result = sum(1, 2)

    expect (result).toBe(3);
})
test("sum function 3", () => {
    const result = sum(1, 2)

    expect (result).toBe(3);
})